# Base URL for the Stitchd gateway REST API
# In development, the Vite proxy rewrites /api → http://localhost:8080
# Set this to your gateway's URL in production builds
VITE_API_BASE_URL=http://localhost:8080
