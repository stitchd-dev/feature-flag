/**
 * Segment domain types — now consolidated in admin/src/lib/types.ts.
 * This file re-exports for backward compatibility with existing imports
 * within the segments/ directory.
 */
export type { SegmentType, Segment, CreateSegmentRequest, UpdateSegmentRequest } from '../../lib/types'
