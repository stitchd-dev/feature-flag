{
  "last_refreshed": "2026-05-22T03:55:00Z",
  "scope": "all",
  "previous_refresh": "2026-05-20T20:30:00Z",
  "changes": {
    "workflow.md": [
      "`cargo sqlx prepare` invocation now includes `-- --tests` — without it, test-only `sqlx::query!`/`sqlx::query_scalar!` macros are silently skipped from the offline cache, which broke CI on the experimentation_full_20260521 merge (12 'no cached data for this query' errors in stitchd-db/tests/experiment.rs). Long-form comment added inline explaining why.",
      "Added a 'File-ownership boundary in worker prompts' subsection under Parallel Sub-Agent Workflow — 14 parallel workers across the experimentation_full track produced zero source-file merge conflicts because each worker prompt explicitly listed owned + forbidden files.",
      "Added 'Beads close gotcha (`--no-auto` doesn't reliably persist)' note — closures may silently re-open; use plain `bd close` and `--force` on phantom-dep errors."
    ],
    "patterns.md": [
      "Refresh marker updated to 2026-05-22.",
      "10 patterns under 'Experimentation Patterns' section added by Phase 11 of the experimentation_full_20260521 track (already present pre-refresh — refresh just acknowledges them)."
    ],
    "product.md": [
      "Already updated by Phase 11 worker — Implementation Status table now includes the new experimentation row (bolded). Refresh confirms; no further changes."
    ],
    "tech-stack.md": [
      "Already updated by Phase 11 worker — ClickHouse Schema section gains `experiment_assignments` + `experiment_assignments_mv` + `experiment_iterations_active` dictionary; `flag_evaluation_log_v2` row mentions the new `targeting_on` + `matched_rule_id` columns. Refresh confirms; no further changes."
    ]
  },
  "tracks_completed_since_last_refresh": [
    "experimentation_full_20260521 (11 phases + 3 P2 bug fixes: feature-flag-buc, feature-flag-11d, feature-flag-1p6) — merged at e9617f5, ARRAY JOIN refactor pushed in 480ccf3"
  ],
  "follow_up_drift_notes": [
    "feature-flag-502 (wisp, closed): CI sqlx-prepare drift — fix shipped in 480ccf3.",
    "No Beads-vs-Conductor drift detected: all 56 child stories + 11 phase milestones + epic (feature-flag-dak) are CLOSED. No orphans."
  ]
}
