# Rust SDK Overview

> This chapter links to the generated rustdoc for the `stitchd-sdk` crate.
> Run `cargo xtask docs` to regenerate.

The Stitchd Rust SDK provides server-side, in-process flag evaluation.

See [Quickstart](./quickstart.md) to get started.
