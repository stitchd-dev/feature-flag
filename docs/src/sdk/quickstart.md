# SDK Quickstart

> This chapter is extracted from `//!` module-level doc comments in `stitchd-sdk/src/lib.rs`.
> Run `cargo xtask docs` to regenerate.

<!-- SDK quickstart content will be populated by cargo xtask docs -->
