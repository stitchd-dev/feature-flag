# 02 — String flag with Eq leaf condition

Tests that a leaf `Eq` condition over a context parameter works. Two requests:
- One context where the rule matches → `matched` outcome.
- One context where the rule does not match → falls through to default rule.
